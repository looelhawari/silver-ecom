import { CmsPage } from "@/components/storefront/cms-page";

export const metadata = { title: "Returns & exchange policy" };

export default function ReturnsPolicyPage() {
  return <CmsPage slug="returns-policy" />;
}
